import { colors } from "../../colors"

export const styles = {
    submitButton: {
        borderWidth: 2,
        borderRadius: 10,
        backgroundColor: colors.secondary,
        borderColor: colors.primary,
        padding: 10,
        margin: 10,
      },
      submitButtonText: {
        fontSize: 24,
        color: colors.white,
      }
}