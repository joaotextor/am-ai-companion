import { colors } from "../../colors"

export const styles = {
    submitButton: {
        borderWidth: 2,
        borderRadius: 10,
        backgroundColor: colors.secondary,
        borderColor: colors.primary,
        padding: 10,
        marginVertical: 10,
        width: "100%",
      },
      submitButtonText: {
        fontSize: 24,
        color: colors.white,
      },
      buttonTextView: {
        alignItems: "center",
      }
}